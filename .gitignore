# local config — never commit
ralph.env
*.local.env

# run artifacts
reports/*
!reports/.gitkeep

# the kill switch is intentionally transient
PAUSE

# os cruft
.DS_Store

# local per-project env files (multi-project mode, --projects-dir)
projects.d/
